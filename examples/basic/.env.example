# Langflow Configuration for the Basic Example Server

# The base URL of your Langflow instance
# Example: LANGFLOW_ENDPOINT_URL=http://127.0.0.1:7860
LANGFLOW_ENDPOINT_URL=

# (Optional) Your Langflow API key, if your instance requires authentication.
# Leave blank if not needed.
LANGFLOW_API_KEY=

# Node Server Configuration
# PORT=3001          # Specific port (optional - if not set, Node.js will pick an available port)
# SERVER_HOST=127.0.0.1  # Server hostname (optional - defaults to 127.0.0.1)
